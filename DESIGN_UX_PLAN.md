# 포트폴리오 디자인·UX 개선 계획

검토일: 2026-09-15 · 기준 커밋: `4becdd0`

## 방향

게임 캐릭터 **Rigging Artist**가 먼저 읽히고, 대표 리깅 영상 → 프로젝트 경험 → 실무 기술 → 연락으로 자연스럽게 이어지게 한다. 기존 다크 배경, 레드·시안 포인트, 정적 HTML/CSS/JS와 4개 언어를 유지한다.

이 문서는 현행 코드 검토를 바탕으로 작성했다. 이전 `RENEWAL.md`는 당시 상태에 대한 기록이며, 이번 작업의 현행 기준은 아래 내용이다. 브라우저 화면 점검은 Computer Use가 현재 URL을 확인하지 못해 중단되었다. 따라서 화면 캡처 비교·모바일 실기기 검증을 완료했다고 간주하지 않는다.

## 이번 구현 범위

| 우선순위 | 확인한 문제 | 변경 | 검수 기준 |
| --- | --- | --- | --- |
| P1 | Hero는 Rigging Artist인데 헤더·푸터·홈 메타데이터는 Technical Animator가 먼저 표시됨 | 공개 페이지 직무 표기 통일 | 5개 공개 페이지에서 Rigging Artist 우선 |
| P1 | Hero 버튼은 전체 영상 목록과 외부 채널로 연결됨 | 3ds Max 리깅 작업으로 직접 연결하고, 보조 버튼은 주요 프로젝트로 연결 | 언어 유지, 한 번의 클릭으로 해당 콘텐츠 접근 |
| P1 | 영상 탭 클릭 후 DOM 재생성으로 포커스 소실 가능 | 클릭·키보드 전환 후 활성 탭 포커스 복원; 탭 목록 이름 및 패널 포커스 제공 | 방향키/Home/End/Enter/Space, 빈 목록, 언어 변경 검증 |
| P1 | 모바일 메뉴가 화면보다 길어질 수 있고 Escape 후 숨겨진 링크에 포커스가 남을 수 있음 | 메뉴 내부 스크롤, Escape 포커스 복원, 화면 크기 변경 처리 | 메뉴 끝까지 접근 가능, 닫은 뒤 포커스가 보이는 위치에 있음 |
| P1 | Work의 본문 바로가기가 영상 필터를 건너뜀 | 필터를 포함한 main으로 이동 | skip link 대상 존재·포커스 가능 |
| P2 | 데스크톱 Skills는 좁은 본문에 3열, 핵심 9개·활용 5개·학습 3개의 열 길이 불균형 | 핵심 기술을 넓은 왼쪽, 활용 경험·학습을 오른쪽에 세로 배치 | 정보와 학습 구분 유지, 모바일 1열, 긴 이름 줄바꿈 |
| P2 | 보조 본문 10–12px, 작은 언어 버튼, 낮은 강조색 포커스 | 본문·라벨 크기 및 터치 영역 개선, 밝은 포커스 표시 | 320px 이상에서 핵심 조작 영역 유지, 고정 헤더에 앵커가 가리지 않음 |
| P2 | 이름의 반복 글리치와 마우스를 따라가는 버튼이 콘텐츠 읽기·조작을 방해할 수 있음 | 이름은 초기 등장만, CTA 위치 안정화; 키보드 포커스 시 reveal 콘텐츠 즉시 노출 | reduced-motion 유지, 포커스된 카드가 투명하게 남지 않음 |
| P2 | SERVA ME 페이지의 Home/Work 탐색 구조가 다른 상세 페이지와 다름 | 동일한 하위 탐색 링크 제공 | 어느 상세 페이지에서도 같은 콘텐츠로 이동 가능 |

## 역할 분담

- 상위 모델: 코드 진단, 범위 결정, 계획 작성, 변경 diff와 회귀 검사 검수.
- `gpt-5.6-luna` / UX: `js/main.js`, `js/media-gallery.js`, 관련 회귀 검사.
- `gpt-5.6-luna` / 디자인: `css/polish.css`, `css/work.css`.
- `gpt-5.6-luna` / 정보 구조: 공개 HTML 5개, `data/translations.js`.
- 에이전트별 파일 소유권을 분리하고 커밋·푸시는 검수 후 상위 모델이 진행한다.

## 보존 조건

- 현재 홈 카드에는 Project TAL만 표시한다. AION 2와 SERVA ME의 `showOnHome: false`를 보존한다. 메뉴: SERVA ME → AION 2 → Project TAL.
- 예전 3ds Max 영상 3개는 해당 카테고리 아래쪽에 유지한다.
- 비공개 처리된 두 회사 영상의 제목·ID를 공개 데이터에 다시 넣지 않는다.
- 현재 Maya, Maya Python, mGear는 학습 영역이다. Control Rig와 RBF Solver는 활용 경험이다. 이미 목록에서 빠진 Deformation / Anatomy를 다시 추가하지 않는다.
- 영상 제목, 경력·성과·담당 업무를 임의로 확장하지 않는다.

## 후속 개선 후보

1. 공개 가능한 리그 조작·변형 검증 화면이 준비되면 대표 프로젝트에 짧은 시각적 breakdown을 추가한다. 공식 트레일러만으로는 본인의 리깅 기여를 직접 확인하기 어렵다.
2. 실제 브라우저에서 1440/1024/768/390/320px, 한국어·일본어·영어·중국어, 200% 확대, 키보드만 사용, reduced-motion을 점검한다. 이번 코드 검증과 별도로 수행해야 한다.
3. 장기적으로 CSS 중복 정의를 정리할 수 있으나, 이번 개선에 불필요한 전체 스타일 재작성은 범위에 포함하지 않는다.

## 검증 기록

1차 구현은 세 하위 모델의 변경을 상위 모델이 검토하고 보완한 상태다.

- `node nav-gallery.test.cjs` 통과: 실제 JS를 VM에서 실행해 탭 클릭 후 포커스, 방향키/Home/End, 빈 패널, 언어 이벤트의 포커스 유지, reduced-motion 스크롤, 모바일 Escape 및 양방향 화면 크기 변경을 검사했다. 실제 브라우저의 네이티브 키보드 동작·스크린리더 검증을 대체하지 않는다.
- 공개 HTML 5개의 로컬 링크·앵커 163개 및 번역 키 122개의 4개 언어 존재 여부 확인.
- `js/`, `data/`의 모든 JS 구문 검사와 `git diff --check` 통과.
- `data/media.js`, `data/projects.js`, `data/profile.js`, `data/timeline.js`가 기준 커밋과 동일함을 확인. 공개 영상 13개 / 3ds Max 9개, 홈 TAL 단독 노출 유지.
- CSS 변경은 중괄호·선택자·우선순위 검토로 확인했다. 새 외부 라이브러리는 추가하지 않았다.
- **미완료:** 브라우저가 URL 검증 오류로 자동 조작을 중단하여 화면 캡처 비교, 모바일 실기기, 확대·스크린리더 검증은 수행하지 못했다. 접근성 표준 전체 준수를 검증한 것은 아니다.
- 통합 시 원격의 `b8f8ff4`(Maya Curve Forge 영상 추가), `0d8727c`(한국어 영상 목록 제목 줄바꿈)를 보존했다. 통합 후 공개 영상은 14개 / 3ds Max 9개이며, 영상 데이터는 원격 버전과 동일하다.
